export enum GameMode {
    Classic = 'Classic',
    Flag = 'Flag',
}

export enum GameMapSize {
    Small = 10,
    Medium = 15,
    Large = 20,
}

export enum ItemsPerMapSize {
    Small = 2,
    Medium = 4,
    Large = 6,
}

export enum StartingPointPerMapSize {
    Small = 2,
    Medium = 4,
    Large = 6,
}

export enum PlayersPerMapSize {
    Small = 2,
    Medium = 4,
    Large = 6,
}
